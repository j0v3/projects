const serviceBlockButton = document.querySelectorAll(".serviceBlockButton");
const serviceInformations = document.querySelectorAll(".serviceInformations");

console.log(serviceBlockButton);
console.log(serviceInformations);

switch (serviceBlockButton){

    case serviceBlockButton[0]: 
        serviceInformations[0].classList.add("active") ;
        break;
    default: NamedNodeMap;

}